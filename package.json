{
  "name": "tic-tac-toe",
  "version": "1.0.0",
  "scripts": {
    "install": "concurrently \"npm install --prefix backend\" \"npm install --prefix frontend\"",
    "build-client": "npm run build --prefix frontend"
  },
  "author": "Tyler Hand",
  "license": "MIT",
  "description": "A multiplayer tic-tac-toe game using the socket.io library and a React.js frontend",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
